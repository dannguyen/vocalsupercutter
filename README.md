# voxsupercut


## Developer notes

Install locally with:

```sh
$ pip install -e .
```

Run the CLI with:

```sh
$ voxcut
```

### Version 0.0.1

Running `voxcut` produces this output:

```
hello world
```
