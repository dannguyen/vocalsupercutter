# vocalsupercutter


## Developer notes

Install locally with:

```sh
$ pip install -e .
```

Run the CLI with:

```sh
$ vocalcuts
```

### Version 0.0.1

Running `vocalcuts` produces this output:

```
hello world
```
